name: ML Benchmark

on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # Paused while the FI vs volatility question is being investigated.
      # - name: Run ML benchmark
      #   run: python -u -m ml.diagnostics.benchmark

      # Already established that volatility is strongly associated
      # with the economic target.
      # - name: Run volatility diagnostics
      #   run: python -u -m ml.diagnostics.volatility_diagnostics

      - name: Run volatility vs FI screening
        run: python -u -m ml.diagnostics.volatility_vs_fi_screening

      # Regime gating did not provide enough additional signal.
      # - name: Run volatility regime diagnostic
      #   run: python -u -m ml.diagnostics.volatility_regime_diagnostic

      # Interaction screening did not provide enough additional
      # validation/OOS signal to keep running it routinely.
      # - name: Run volatility interaction screening
      #   run: python -u -m ml.diagnostics.volatility_interaction_screening
